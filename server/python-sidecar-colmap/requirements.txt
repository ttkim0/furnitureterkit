# COLMAP + Open3D sidecar deps. COLMAP itself is a system binary —
# installed via apt-get / brew / Dockerfile, NOT pip.
fastapi==0.115.4
uvicorn[standard]==0.32.0
python-multipart==0.0.12
numpy>=1.26
open3d>=0.18
