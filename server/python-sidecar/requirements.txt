# Sidecar deps. The actual SLAM3R and SpatialLM dependencies are installed
# separately via their own requirements.txt / pyproject.toml — this file
# only covers the FastAPI wrapper itself plus the alignment helpers.
fastapi==0.115.4
uvicorn[standard]==0.32.0
python-multipart==0.0.12
numpy>=1.26
plyfile>=1.0
